// utility.js
export const convertToZAR = (amount) => {
  // Replace this conversion logic with the actual conversion rates
  // This is just a placeholder
  const conversionRate = 1.0; // Example conversion rate
  const convertedAmount = amount * conversionRate;
  return convertedAmount.toFixed(2); // Rounds to 2 decimal places
};
